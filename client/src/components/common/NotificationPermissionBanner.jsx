// NotificationPermissionBanner is intentionally disabled.
// Notification permissions are now managed from the Settings panel in the Sidebar.
export default function NotificationPermissionBanner() {
  return null;
}
