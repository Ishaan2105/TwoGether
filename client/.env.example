# Optional — in dev the Vite proxy forwards /api to http://localhost:5000 automatically.
# Set this only when the API is hosted elsewhere (e.g. deployed backend).
VITE_API_URL=http://localhost:5000/api